# Log-analyzer
Log Analyzer is a Python-based security monitoring tool that parses authentication and web server logs to detect suspicious activity. It identifies brute-force attacks, port scans, privilege escalation, after-hours logins, path traversal attempts, and access to sensitive resources while generating detailed security alerts.
